name: "CodeQL Analysis"

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  codeql:
    name: Codeql
    uses: nasa/cFS/.github/workflows/codeql-reusable.yml@dev
    with:
      component-path: tools/elf2cfetbl
      prep: 'true'
      make: 'make -C tools/elf2cfetbl'
